<Window x:Class="ImageSquared.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageSquared.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Menu>
            <MenuItem Header="File">
                <MenuItem Header="Open"></MenuItem>
            </MenuItem>
            <MenuItem Header="Edit"></MenuItem>
            <MenuItem Header="View"></MenuItem>
            <MenuItem Header="Window"></MenuItem>
            <MenuItem Header="Help"></MenuItem>
        </Menu>
        <Button Content="Select Image"
                HorizontalAlignment="Left"
                Margin="10,31,0,0"
                VerticalAlignment="Top"
                Click="Button_Click"
                RenderTransformOrigin="1.194,0.719">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="Beige"/>
                    <Setter Property="Cursor" Value="Hand"/>

                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="Bisque"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>

            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Width="100"
                            Height="25"
                            CornerRadius="12"
                            Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Image x:Name="SelectedImage" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,80,0,0" Width="389" Height="230"/>
        <Image x:Name="TransformedImage" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="494,80,0,0" Width="240" Height="240"/>
    </Grid>
</Window>
